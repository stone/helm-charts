apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "meerkat-crm.fullname" . }}-test-connection"
  labels:
    {{- include "meerkat-crm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "meerkat-crm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
