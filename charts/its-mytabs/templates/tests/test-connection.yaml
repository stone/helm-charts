apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "its-mytabs.fullname" . }}-test-connection"
  labels:
    {{- include "its-mytabs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "its-mytabs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
